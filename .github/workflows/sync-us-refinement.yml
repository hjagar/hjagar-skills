name: Sync us-refinement mirror

on:
  push:
    branches:
      - main
    paths:
      - 'skills/us-refinement/**'
  workflow_dispatch:

jobs:
  sync:
    name: Sync us-refinement subtree
    runs-on: ubuntu-latest
    steps:
      - name: Checkout monorepo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Push subtree to mirror repo
        env:
          TOKEN: ${{ secrets.SYNC_PAT || secrets.GH_PAT || secrets.GITHUB_TOKEN }}
        run: |
          git remote add mirror "https://x-access-token:${TOKEN}@github.com/hjagar/us-refinement.git"
          git subtree push --prefix=skills/us-refinement mirror main

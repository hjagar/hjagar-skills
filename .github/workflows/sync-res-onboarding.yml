name: Sync res-onboarding mirror

on:
  push:
    branches:
      - main
    paths:
      - 'skills/res-onboarding/**'
  workflow_dispatch:

jobs:
  sync:
    name: Sync res-onboarding subtree
    runs-on: ubuntu-latest
    steps:
      - name: Checkout monorepo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Push subtree to mirror repo
        env:
          TOKEN: ${{ secrets.SYNC_PAT || secrets.GH_PAT || secrets.GITHUB_TOKEN }}
        run: |
          git subtree push --prefix=skills/res-onboarding "https://x-access-token:${TOKEN}@github.com/hjagar/res-onboarding.git" main
